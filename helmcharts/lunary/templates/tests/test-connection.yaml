apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "lunary.fullname" . }}-test-connection"
  labels:
    {{- include "lunary.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "lunary.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
